define([

], function() {
	describe("some test", function() {

		it("fails one test", function() {
			expect(1).toEqual(1);
		});

		it("passes another one", function() {
			expect(1).toEqual(1);
		});
	});
});
